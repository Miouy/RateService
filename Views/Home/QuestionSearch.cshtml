@model IEnumerable<rateservice.Models.Question>


@foreach (var question in Model)
{
    <div class="card mb-3">
        <form action="#" method="post">
            <div class="card-body">
                <h5 class="card-title">@question.QuestionTitle</h5>
                <p class="card-text">@question.QuestionText</p>
                <div class="form-group d-flex">
                    <input type="text" class="form-control" placeholder="Answer to question" />
                    <div class="input-group-append">
                        <button class="btn btn-outline-secondary" type="button">Send</button>
                    </div>
                </div>
                <p>Anonymously</p>
            </div>
            <div class="card-footer text-muted">
                <p>Posted by <a href="#" style="color: #180b7a;">@question.User.FirstName</a></p>

            </div>
        </form>
    </div>
}
